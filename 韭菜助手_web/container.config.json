{
  "containerPort": 80,
  "dockerfilePath": "Dockerfile",
  "buildDir": ".",
  "releaseLimit": 1,
  "minInstances": 0,
  "maxInstances": 10,
  "cpu": 0.5,
  "mem": 1,
  "policy": "TIMEOUT",
  "customLogs": "stdout",
  "envParams": {
    "PORT": "80"
  }
}
